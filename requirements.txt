# On the pod (runpod-torch-v280), torch/torchvision/numpy/pillow come with the image and are left as-is.
torch>=2.0
torchvision>=0.15
numpy>=1.24
scikit-learn>=1.3
pillow>=9.0
scipy>=1.10
matplotlib>=3.7
pyyaml>=6.0
pytest>=7
# ripser/persim: only the legacy topology axes of Upgraded-Mod used them; atlas/ does not.
# B1 (ImageNet / ViT; docs/plans/B1_VIT_MARGIN.md, integration D4), pinned. None of them requires numpy, scipy or
# scikit-learn (PyPI requires_dist, 2026-09-23), so the CIFAR instrument's libraries do not move; the pre-launch version
# check and A4b G0d (versions.json) compare them with the committed sessions.
pyarrow==25.0.1          # parquet reader for the HF mirror; never the `datasets` library (CLAUDE.md)
huggingface_hub==1.32.0  # snapshot_download / hf_hub_download at a pinned revision
safetensors==0.8.0       # timm weight file format
timm==1.0.29             # DeiT-B; 1.0.30 (uploaded 2026-09-22) is not used
